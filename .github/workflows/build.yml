name: Build OpenWrt for JDCloud RE-SS-01 (Final Fix)

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: master
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 准备环境
      uses: actions/checkout@v4

    - name: 安装依赖与清理空间
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install $(curl -fsSL https://is.gd/depends_ubuntu_2204)
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"

    - name: 克隆源码
      run: |
        # 直接在工作目录克隆，避免路径混乱
        git clone $REPO_URL -b $REPO_BRANCH openwrt

    - name: 更新 & 安装 Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 写入配置 (针对 RE-SS-01)
      run: |
        cd openwrt
        echo "CONFIG_TARGET_ipq60xx=y" >> .config
        echo "CONFIG_TARGET_ipq60xx_generic=y" >> .config
        echo "CONFIG_TARGET_ipq60xx_generic_DEVICE_jdcloud_re-ss-01=y" >> .config
        
        # 集成 TikTok 核心优化插件
        echo "CONFIG_PACKAGE_luci-app-turboacc=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-openclash=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-upnp=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
        
        make defconfig

    - name: 下载软件包
      run: |
        cd openwrt
        make download -j8

    - name: 编译固件
      run: |
        cd openwrt
        # 多线程编译，失败则转单线程输出详细日志
        make -j$(nproc) || make -j1 V=s

    - name: 自动化搜寻固件 (核心修复步)
      run: |
        mkdir -p ./artifact/
        # 搜索所有生成的 bin 或 img 固件并移动到 artifact 目录
        find openwrt/bin/targets/ -type f -name "*re-ss-01*" -o -name "*sysupgrade.bin*" | xargs -I {} cp {} ./artifact/
        echo "FIRMWARE=$PWD/artifact" >> $GITHUB_ENV

    - name: 上传固件到 Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: JDCloud_RE-SS-01_Firmware
        path: ${{ env.FIRMWARE }}
